services:
  seatsniper:
    build: .
    container_name: seatsniper
    restart: unless-stopped
    env_file: .env
    environment:
      - DB_PATH=/data/seatsniper.db
    volumes:
      - seatsniper_data:/data

volumes:
  seatsniper_data:
